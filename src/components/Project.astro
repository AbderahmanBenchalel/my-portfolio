---
import Container from "./ui/Container.astro";
import Flex from "./ui/Flex.astro";
import Image from "./ui/Image.astro";
import projectImage from "../../public/project-one.png";
import Tags from "./ui/Tags.astro";
import Heading from "./ui/Heading.astro";
import Text from "./ui/Text.astro";
import FlexItem from "./ui/FlexItem.astro";

const {data} = Astro.props;

const {
title,
tags,
thumbnail,
description,
demo,
code
} = data;
---

<Container>
  <Flex wrap="wrap" gap="4.5rem">
    <FlexItem flex="2 1 20rem">
      <Image src={thumbnail}/>
    </FlexItem>
    
    <FlexItem flex="3 1 45rem">

      <Tags marginBottom="2.5rem" tags={tags}/>
      <Heading marginBottom="1.8rem">{title}</Heading>
      <Text marginBottom="4rem">{description}</Text>
      <span class="links">
        <a class="demoLink" href={demo}>Demo</a>
        <a class="codeLink" href={code}>Code</a>
      </span>
  </FlexItem>
  </Flex>
</Container>


<style>
  .links{
  }

  .links a{
      font-size: 18px;
      font-weight: 500;
      text-decoration: none;
      border-radius: 12px;
  }

  .demoLink{
    display: inline-block;
    color: white;
    background-color: var(--color-primary);
    padding: 13px 38px;
    margin-right: 1.1rem;
  }

  .codeLink{
    color: var(--color-primary);
    background-color: white ;
    border: 1px solid var(--color-primary);
    padding: 12px 35px;
  }
</style>