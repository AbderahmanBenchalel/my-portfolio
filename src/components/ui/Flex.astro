---
export interface Props{
  justify?:string,
  align?:string,
  gap?:string,
  wrap?:string,
  Element?:string,
  direction?:string
}

const {align='', gap='', wrap='', justify='', Element='div',direction='row'} = Astro.props as Props;
---

<Element class="flex" style=`align-items: ${align}; gap: ${gap}; flex-wrap: ${wrap}; justify-content: ${justify}; flex-direction: ${direction}; `>
  <slot/>
</Element>

<style>
  .flex{
    display: flex;
  }
</style>